import u from 'updeep';

import weaponry from './weaponry/inflate';

export default function inflate_bogey(shorthand) {
    return u(
        {
            weaponry
        },
        shorthand,
    );
}
