
/*
import { Structure } from "./types";
import Reaction from "../../../../reducer/reaction";
import { RootAction } from "../../../actions";
import { damage } from "../weaponry/middleware/actions";

export const reaction = new Reaction({
  hull: { current: 0, rating: 0 }
} as Structure);



export default reaction.asReducer;

*/
