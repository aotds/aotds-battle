import u from 'updeep';
import inflate_bogey from './bogey/inflate';
import {Bogey_Shorthand} from './bogey/types';

export type Bogeys_Shorthand = Bogey_Shorthand[];

export default function inflate_bogeys(shorthand) {
    return u.map(inflate_bogey, shorthand);
}
